// Staging credentials are intentionally left blank here.
// Provide real values via a local, gitignored override (e.g. environment.local.ts)
// or a build-time secret injection step. Never commit real credentials.
export const environment = {
  apiBaseUrl: 'https://your-api.example.com',
  stagingUsername: '',
  stagingPassword: ''
};
